# ocr_core/.env.example
# Pure standalone OCR execution engine.
# No network or cloud credentials required.

# CPU threading constraints (Recommended for PaddleOCR on CPU to prevent thrashing)
OMP_NUM_THREADS=1
MKL_NUM_THREADS=1
